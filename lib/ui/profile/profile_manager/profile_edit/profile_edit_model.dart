import 'package:lomo/ui/base/base_model.dart';

class ProfileEditModel extends BaseModel {
  @override
  ViewState get initState => ViewState.loaded;

  @override
  void dispose() {
    super.dispose();
  }
}
